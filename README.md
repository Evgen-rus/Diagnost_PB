# Диагност ПБ - Telegram-бот по неразрушающему контролю

Telegram-бот для обучения специалистов в области неразрушающего контроля (НК), предоставляющий информацию по стандартам, технологиям и методикам НК.

## 📋 Описание

"Диагност ПБ" - это интеллектуальный ассистент, созданный для помощи специалистам различного уровня подготовки в области неразрушающего контроля. Бот использует API OpenAI для генерации точных, содержательных ответов на вопросы пользователей.

### Основные возможности:

- **Консультации по методам НК**: ультразвуковой, радиографический, магнитно-порошковый, капиллярный, вихретоковый, акустико-эмиссионный, тепловой, визуальный и измерительный контроль
- **Информация по стандартам**: ГОСТы, ISO, ASTM и другие нормативные документы
- **Адаптация ответов**: автоматическая настройка сложности объяснений в зависимости от уровня специалиста
- **Интерактивный интерфейс**: встроенные кнопки для быстрого доступа к функциям
- **Глубокие объяснения**: возможность детального разбора сложных концепций

## 🎯 Интерактивный анализатор Excel файлов

Полностью интерактивный анализатор для работы с Excel файлами. Выбирайте режим и файл через удобные меню!

### Основные возможности:
- **🎯 Двухэтапное интерактивное меню**: сначала выбор режима, затем файла
- **👁️ Быстрый просмотр**: мгновенный обзор структуры без создания отчетов
- **🔥 Полный анализ**: статистика + визуализации + отчет
- **⚡ Быстрый анализ**: анализ без графиков для больших файлов  
- **📊 Анализ без GUI**: для серверов без графического интерфейса
- **🔄 Циклическая работа**: анализируйте несколько файлов подряд

### Быстрый старт:
```bash
# Интерактивное меню: выбор режима и файла по номерам
python analyze_database.py

# Прямой анализ конкретного файла
python analyze_database.py my_data.xlsx
```

📖 **Документация**: `INTERACTIVE_GUIDE.md`

## 🚀 Технологии

- **Python 3.10+**
- **aiogram 3.17.0**: фреймворк для создания Telegram-ботов
- **OpenAI API**: для генерации ответов (модель gpt-4.1-nano по умолчанию)
- **SQLite**: для логирования диалогов и хранения данных
- **tiktoken**: оптимизация использования токенов
- **pandas, matplotlib, seaborn**: для анализа Excel файлов

## 🔧 Установка и запуск

### Предварительные требования

- Python 3.10 или выше
- Telegram Bot Token (получается через [@BotFather](https://t.me/BotFather))
- OpenAI API ключ

### Установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/username/Diagnost_PB.git
cd Diagnost_PB
```

2. Создайте и активируйте виртуальное окружение:

```bash
python -m venv venv
# На Windows
venv\Scripts\activate
# На Linux/MacOS
source venv/bin/activate
```

3. Установите зависимости:

```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

5. Отредактируйте файл `.env`, добавив свои данные:

```
TELEGRAM_BOT_TOKEN=your_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=ваша_модель_openai
LOG_LEVEL=INFO
ENABLE_DIALOG_LOGGING=true
```

### Запуск бота

```bash
python bot.py
```

## 📊 Структура проекта

- **bot.py**: основной файл с обработчиками команд и сообщений
- **prompts.py**: системные промпты и инструкции для модели
- **config.py**: конфигурация проекта и загрузка переменных окружения
- **utils/**: вспомогательные модули
  - **keyboards.py**: создание и управление клавиатурами Telegram
  - **logger.py**: система логирования
  - **token_counter.py**: оптимизация использования токенов
  - **database.py**: работа с базой данных
  - **session_cleaner.py**: очистка неактивных сессий
  - **exceptions.py**: обработка исключений
- **data/**: хранение данных
  - **bot_dialogs.db**: база данных диалогов
- **logs/**: журналы работы бота

## 📝 Уровни специалистов

Бот поддерживает три уровня специалистов и адаптирует ответы в соответствии с выбранным уровнем:

1. **Уровень 1 (Технический)**: базовый уровень знаний, работа под руководством, выполнение стандартных процедур.
2. **Уровень 2 (Квалифицированный)**: самостоятельное проведение контроля, интерпретация результатов, обеспечение соответствия стандартам.
3. **Уровень 3 (Экспертный)**: глубокое понимание методик, разработка процедур контроля, сертификация и обучение.

## 📋 Команды бота

- `/start` - Начать диалог с ботом
- `/cancel` - Прервать текущее взаимодействие

## 🔍 Функциональные кнопки

- **Объясни** - получить развернутое объяснение по текущей теме
- **Новый вопрос** - сбросить контекст и начать новый диалог
- **Выбрать уровень специалиста** - настроить сложность ответов под ваш уровень

## 🤝 Вклад в проект

Приветствуются предложения по улучшению бота. Для этого:

1. Создайте форк проекта
2. Создайте ветку для вашей функции: `git checkout -b feature/amazing-feature`
3. Зафиксируйте изменения: `git commit -m 'Добавлена новая функция'`
4. Отправьте изменения в ваш форк: `git push origin feature/amazing-feature`
5. Создайте Pull Request


## 🧪 Тестирование

Проект включает систему тестирования нейроконсультанта, которая позволяет оценивать качество ответов, собирать метрики производительности и анализировать результаты.

### Особенности системы тестирования

- **Проведение одиночных тестов**: задайте вопрос и оцените ответ
- **Пакетное тестирование**: запустите серию тестов из файла с вопросами
- **Сбор метрик**: автоматическое измерение количества токенов, времени и стоимости
- **Экспорт данных**: сохранение результатов в CSV-файл и базу данных
- **Оценка качества**: система сбора обратной связи от тестировщиков

### Как запустить тестирование

#### Подготовка

1. Убедитесь, что бот настроен и работает корректно
2. Активируйте виртуальное окружение:

```bash
# На Windows
venv\Scripts\activate
# На Linux/MacOS
source venv/bin/activate
```

#### Одиночный тест

Для проведения одиночного теста выполните:

```bash
python test_runner.py --question "Ваш вопрос здесь" --output "путь/к/файлу_результатов.csv"
```

Для сохранения результатов в Excel вместо CSV используйте расширение .xlsx:

```bash
python test_runner.py --question "Ваш вопрос здесь" --output "путь/к/файлу_результатов.xlsx"
```

Пример:
```bash
python test_runner.py --question "Какие методы ультразвукового контроля существуют?" --output "results/test1.xlsx"
```

#### Тестирование из файла с вопросами

1. Создайте текстовый файл с вопросами (по одному на строку), например `questions.txt`:
```
Что такое капиллярный контроль?
Какие дефекты можно обнаружить методом магнитно-порошкового контроля?
Какие стандарты регулируют радиографический контроль сварных соединений?
```

2. Запустите тестирование, указав расширение .xlsx для создания Excel-файла:
```bash
python test_runner.py --file "путь/к/questions.txt" --output "путь/к/результатам.xlsx"
```

Пример:
```bash
python test_runner.py --file "tests/questions.txt" --output "results/batch_test.xlsx"
```

#### Указание ID тестировщика

Вы можете указать свой ID для группировки результатов:

```bash
python test_runner.py --question "Ваш вопрос" --tester 42 --output "результаты.xlsx"
```

#### Экспорт всех результатов тестирования

Для экспорта всех результатов из базы данных в Excel-файл:

```bash
python test_runner.py --export "путь/к/полным_результатам.xlsx"
```

### Преимущества Excel-формата

Excel-файлы имеют ряд преимуществ по сравнению с CSV:

- **Удобное форматирование**: текст в ячейках автоматически переносится, столбцы имеют оптимальную ширину
- **Фильтрация данных**: встроенные фильтры позволяют быстро анализировать результаты
- **Сохранение форматирования**: поддерживаются выделение текста и другие элементы форматирования
- **Совместимость**: файлы открываются в Microsoft Excel, LibreOffice Calc и других подобных программах

### Процесс тестирования

1. После запуска теста вы увидите вопрос и полученный от бота ответ
2. Система покажет метрики (токены, стоимость, время генерации)
3. Вам будет предложено оценить ответ по шкале от -2 до +2:
   - **-2** — очень плохой ответ, содержит критические ошибки
   - **-1** — неудовлетворительный ответ, есть неточности
   - **0** — нейтральный ответ, не хороший, но и не плохой
   - **+1** — хороший ответ с несущественными недостатками
   - **+2** — отличный, полный и точный ответ
4. После оценки вам будет предложено оставить комментарий
5. Данные сохраняются в базу и CSV-файл (если указан)

### Примеры использования

#### Пример 1: Проверка ответов на стандартные вопросы

```bash
# Создаем директорию для результатов
mkdir -p results

# Запускаем тест с конкретным вопросом
python test_runner.py --question "Опишите основные методы ультразвукового контроля" --output "results/uzt_test.csv" --tester 1

# Запускаем тесты из файла
python test_runner.py --file "tests/standard_questions.txt" --output "results/standard_tests.csv" --tester 1
```

#### Пример 2: Сравнение качества ответов до и после обновления бота

1. Создайте файл с тестовыми вопросами `benchmark_questions.txt`
2. Проведите тестирование перед обновлением:
   ```bash
   python test_runner.py --file "tests/benchmark_questions.txt" --output "results/before_update.csv" --tester 2
   ```
3. После внесения изменений проведите те же тесты:
   ```bash
   python test_runner.py --file "tests/benchmark_questions.txt" --output "results/after_update.csv" --tester 2
   ```
4. Сравните результаты, чтобы оценить эффективность изменений

### Структура CSV-файла с результатами

CSV-файл с результатами тестирования содержит следующие столбцы:

| Столбец | Описание |
|---------|----------|
| Вопрос | Тестовый вопрос |
| Ответ | Полный ответ бота |
| Оценка | Числовая оценка от -2 до +2 |
| Токены | Количество использованных токенов |
| Комментарий | Комментарий тестировщика |
| Время | Время генерации ответа в секундах |
| Цена | Стоимость генерации ответа в долларах |
| Чанки | Использованные фрагменты данных (JSON) |

При экспорте всех результатов из базы данных файл также содержит столбцы:
- **ID** - уникальный идентификатор теста
- **Дата и время** - когда был проведен тест
- **ID тестировщика** - идентификатор человека, проводившего тест

### Анализ результатов

После завершения серии тестов система выводит статистику:
- Средняя оценка ответов
- Среднее количество токенов на ответ
- Среднее время генерации
- Общая стоимость тестирования

Результаты в CSV-файле можно открыть в Excel или другом редакторе таблиц для дальнейшего анализа.

#### Интерпретация результатов

При анализе результатов обратите внимание на:

1. **Средняя оценка**:
   - < 0: бот нуждается в серьезной доработке
   - 0-1: бот работает удовлетворительно, но есть пространство для улучшений
   - > 1: бот выдает хорошие результаты

2. **Количество токенов и стоимость**:
   - Высокие значения могут указывать на неэффективность промптов
   - Большой разброс значений может говорить о непредсказуемом поведении бота

3. **Время генерации**:
   - Долгое время ответа может негативно влиять на пользовательский опыт
   - Ищите оптимальный баланс между качеством и скоростью

#### Советы по улучшению бота на основе тестирования

- **Частые ошибки**: проанализируйте ответы с низкими оценками, чтобы выявить закономерности
- **Соотношение оценка/токены**: ищите оптимальный баланс между точностью и эффективностью
- **Тематический анализ**: группируйте тесты по темам, чтобы выявить проблемные области
- **Сравнительный анализ**: регулярно проводите тестирование на одном и том же наборе вопросов для отслеживания прогресса

### Создание своих тестовых сценариев

Для создания эффективных тестов:
1. Подготовьте разнообразные вопросы, охватывающие разные темы и уровни сложности
2. Включите как типичные, так и нестандартные запросы
3. Сгруппируйте вопросы по категориям в отдельных файлах
4. Сохраняйте результаты в отдельные CSV-файлы для каждой категории

Рекомендуемая структура тестовых файлов:
```
tests/
├── basic_questions.txt      # Базовые вопросы о неразрушающем контроле
├── standards_questions.txt  # Вопросы о стандартах и нормативах
├── technical_questions.txt  # Технические вопросы по оборудованию
└── edge_cases.txt           # Нестандартные и сложные вопросы
```

## 👤 Автор

**Расюк Евгений**

---

## 📜 Лицензия

Проект распространяется под лицензией MIT. Подробнее см. файл LICENSE.